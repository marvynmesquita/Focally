services:
  - type: web
    name: focally
    env: static
    buildCommand: git clone https://github.com/flutter/flutter.git -b stable && ./flutter/bin/flutter build web
    staticPublishPath: build/web
